services:
  mysql-test:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: test_root_pass
    ports:
      - "3307:3306"
    tmpfs: /var/lib/mysql   # ephemeral – no data persists after stop